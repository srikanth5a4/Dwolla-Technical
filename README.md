This repo consists of script file which is used to parse the log file and finds the ip's of REJECT entries and list the total no.of reject entries.

Input 1:script.sh
Input 2:s3://dwolla-technical-exercise
Output :20000.log.json

Command to run the script as follows:

sh script.sh 20000.log s3://dwolla-technical-exercise
